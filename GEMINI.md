# Gemini CLI Project Instructions

- **Commit Attribution:** Always include "committed by gemini" in the commit message so other agents (like Claude) can identify these changes.
- **Documentation & Standards:** Prioritize using the project's existing documentation and standards files (e.g., `CLAUDE.md`, `.claude/agent-memory/`) over creating new Gemini-specific ones. Use `GEMINI.md` only for core operational instructions specifically for this agent.
- **Follow Claude Best Practices:** Adhere to the patterns and rules defined in `CLAUDE.md`.
