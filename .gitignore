*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
app/build/
/captures
.externalNativeBuild
.cxx
*.keystore
!debug.keystore
local.properties

# Firebase — never commit these to a public repo
google-services.json
secrets/

# Signing keystore — never commit to public repo
keystore/

# PoarVault backend — AWS credentials and Plaid secrets
infra/.env
infra/node_modules/
infra/lambda/node_modules/

# Duplicate local.properties files
local.properties.*
